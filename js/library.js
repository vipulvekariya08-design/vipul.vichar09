async function loadLibrary(){let el=document.querySelector('#writing-list');if(!el)return;let data=await fetch('data/writings.json').then(r=>r.json());function render(a){el.innerHTML=a.map(x=>`<article class="card"><img class="cover" src="${x.cover}" alt="${x.title} cover"><div class="meta">${x.category} · ${x.language}</div><h2>${x.title}</h2><p>${x.description}</p><a class="btn" href="reader.html?id=${x.id}">Read</a></article>`).join('')||'<p>No writings found.</p>'}render(data);let s=document.querySelector('#search');if(s)s.oninput=()=>{let q=s.value.toLowerCase();render(data.filter(x=>JSON.stringify(x).toLowerCase().includes(q)))}}document.addEventListener('DOMContentLoaded',loadLibrary);